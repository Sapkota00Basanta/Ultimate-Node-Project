{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "restart backend",
      "type": "shell",
      "command": "docker compose restart backend", // here backend is the name of docker service
      "presentation": {
        "reveal": "never" // Never show output of this task result
      }
    }
  ]
}
